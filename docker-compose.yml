version: "3.9"

services:

  qdrant:

    image: qdrant/qdrant:latest

    container_name: qdrant

    ports:
      - "6333:6333"
      - "6334:6334"

  app:

    build: .

    ports:
      - "8080:8080"

    depends_on:
      - qdrant

    environment:
      OLLAMA_API_KEY: ${OLLAMA_API_KEY}